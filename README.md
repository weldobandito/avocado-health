# Avocado Health — Creator Booking Page

Landing page for the pediatrician-creator partnership offer: a private, evidence-backed text line built and run for the creator, in their name and voice.

## Files

- `index.html` — working source. Loads images from `assets/`.
- `avocado-health-booking-page.html` — standalone export with all images embedded as base64. Open directly in any browser, no server or asset folder needed. Send this version if sharing the file itself.
- `assets/` — logo, brandmark, and photography used by `index.html`.

## Before this goes live

- All "Book a call" buttons currently link to `mailto:hans@avocadohealth.ai`. Swap in the real scheduler link before launch (search for `mailto:` in `index.html`).
